# 開発ルール

## TDD（テスト駆動開発）

機能追加・バグ修正・リファクタリングを問わず、すべての実装はTDDで行う。

**REQUIRED SKILL:** `test-driven-development` を必ず使うこと。

```
NO PRODUCTION CODE WITHOUT A FAILING TEST FIRST
```

例外はない。「シンプルだから」「急いでいるから」はすべて合理化。

## プロジェクトルールの適用

スキル（brainstorming・executing-plans・subagent-driven-development・receiving-code-review等）を呼び出す前に、必ず `docs/rules/project-rules.md` を Read ツールで読み込み、記載されているルールをその作業全体に適用すること。

ファイルが存在しない場合は通常通り進める。
