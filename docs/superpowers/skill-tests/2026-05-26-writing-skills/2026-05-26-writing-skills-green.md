# GREEN テスト — writing-skills テスト結果保存先の追加

**日時:** 2026-05-26
**対象スキル:** writing-skills（既存スキル編集）
**変更内容:** スキルテスト結果を `docs/superpowers/skill-tests/` に保存する指示の追加
**フェーズ:** GREEN（スキル編集後の検証）

---

## シナリオ

編集後のスキルファイルを読ませたエージェントに以下を質問した：

1. REDテスト結果ファイルの保存先は？（パターンで回答）
2. GREENテスト結果ファイルの保存先は？
3. テスト中に生成されたファイルはどこに保存するか？
4. テスト結果ファイルの命名規則は？
5. ファイルヘッダーに必要なフィールドは？

また、「my-new-skill」の2026-05-26付けREDテスト完了後のファイルパスと内容をシミュレートさせた。

## サブエージェントの出力（要約）

- 保存先: `docs/superpowers/skill-tests/{date}-{skill-name}/{date}-{skill-name}-red.md` と正確に回答（スキルファイルから読み取り）
- GREEN保存先: `docs/superpowers/skill-tests/{date}-{skill-name}/{date}-{skill-name}-green.md` と正確に回答
- 生成ファイル: 「同じフォルダにコピーする」と正確に回答
- 命名規則: `{YYYY-MM-DD}-{skill-name}-{phase}.md`、反復時は `-v2`/`-refactor2` と正確に回答
- ヘッダーフィールド: `日時`・`対象スキル`・`変更内容`・`フェーズ` の4フィールドを正確に列挙
- シミュレーション: 正しいパスとフォーマットでファイルを作成した
- 特筆: リポジトリ構造からの推測ではなく、スキルファイルの記載を根拠として回答した

## 判定

**PASS** — すべての質問にスキルファイルの記載から正確に回答できた

- ✅ 保存先ディレクトリをスキルから読み取れた
- ✅ ファイル命名規則をスキルから読み取れた
- ✅ 生成ファイルの扱いをスキルから読み取れた
- ✅ ファイルフォーマット（必須フィールド）をスキルから読み取れた
- ✅ チェックリストの保存タスクを認識した
- ✅ シミュレーションで正しいパス・内容を出力した
