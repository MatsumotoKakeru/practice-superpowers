# プロジェクトルール

## コーディングルール
- `useEffect` の依存配列には、コールバック内で参照しているすべての変数・関数を必ず含めること（ESLint の `exhaustive-deps` ルールに従う）
- コンポーネントのpropsは `interface` で定義する（`type` ではなく）

## 設計決定事項
- **React Query**: APIキャッシュ管理に使用する（理由: オフラインサポートとリトライ機能が必要なため）

## レビュー指摘パターン

## 環境・構成

## 共通事項
